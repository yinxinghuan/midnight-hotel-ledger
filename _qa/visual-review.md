# 视觉与运行验收

- 主验收尺寸：390×844；窄屏尺寸：320×568。
- platform-layout：生产访客脚本保留，仅由 QA 注入样式隐藏外部访客栏。
- external-guest：访客栏可见时，夜班终端仍然可见且可以继续操作。
- 三条处理代码均验证了选择、视频演出中段、报告卡；最终收集为 3/3，并显示“307号案卷封存”。
- 视频资产均为 768×1024、H.264/AAC、24fps、约 5.04 秒，三条文件哈希不同。
- 主动拦截 `plug_cinema.mp4` 后，对应静帧回退正常显示，流程不中断。
- 320×568 的方案按钮实测高度为 44.417px，满足触控门槛。
- 除故障注入产生的预期媒体错误外，浏览器 console/pageerror：0。
- UI 静态审计：无功能 Emoji；图标使用项目内统一 SVG。
- P0/P1：无。

截图证据位于 `_qa/ui/`，视频关键帧证据位于 `_qa/video-frames/`。

