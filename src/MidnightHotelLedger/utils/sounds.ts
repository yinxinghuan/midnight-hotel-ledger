import type{EndingId}from'../types';let ctx:AudioContext|null=null;function ac(){ctx??=new AudioContext();if(ctx.state==='suspended')void ctx.resume();return ctx}function tone(f:number,d=.1,type:OscillatorType='sine',g=.03,delay=0){try{const c=ac(),o=c.createOscillator(),v=c.createGain(),s=c.currentTime+delay;o.type=type;o.frequency.setValueAtTime(f,s);v.gain.setValueAtTime(.0001,s);v.gain.exponentialRampToValueAtTime(g,s+.012);v.gain.exponentialRampToValueAtTime(.0001,s+d);o.connect(v).connect(c.destination);o.start(s);o.stop(s+d+.03)}catch{/* non-fatal */}}export function power(){tone(90,.36);tone(240,.25,'sine',.025,.12)}export function choose(){tone(380,.07,'triangle')}
export function beat(id:EndingId,n:number){if(n===1){tone(120,.12);tone(105,.12,'sine',.03,.16)}else if(n===2){id==='plug'?tone(80,.42,'sawtooth',.045):id==='maintenance'?[620,930].forEach((f,i)=>tone(f,.16,'sine',.032,i*.11)):[310,465,620].forEach((f,i)=>tone(f,.28,'triangle',.025,i*.05))}else if(n===3)tone(id==='plug'?46:id==='maintenance'?180:520,.28,id==='plug'?'sawtooth':'triangle',.045);else if(n===4)tone(72,.16,'square',.045)}
